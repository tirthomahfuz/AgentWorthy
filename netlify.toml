[build]
  base = "apps/web"
  command = "cd ../.. && npm install && npm run build:web"
  publish = "apps/web/.next"

[build.environment]
  NODE_VERSION = "22"

# Next.js App Router — Netlify runtime handles SSR/API routes automatically
[[plugins]]
  package = "@netlify/plugin-nextjs"
